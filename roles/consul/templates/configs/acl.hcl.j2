{% if consul_enable_acl | bool %}
acl = {
    enabled = true
    default_policy = "deny"
    enable_token_persistence = true
    tokens {
      "agent" = "{{consul_acl_agent_token}}"
      "default" = "{{consul_acl_default_token}}"
    }
}
{% endif %}
